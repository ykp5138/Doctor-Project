# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment / secrets
.env
.env.local

# Python
.venv/
venv/
__pycache__/
*.pyc

# Generated outputs
outputs/
*_transcription.html
*_transcription.txt
confidence_*.json
reconciled_*.json

# Patient source recordings are .MOV uploads — block PHI media so it can never
# be committed. (The tracked .mp3 YouTube test files under audio/ are NOT ignored.)
*.mov
*.MOV

# Generated semantic-search indexes — rebuild BOTH with:
#   python build_icd_index.py  AND  python build_icd_index_sapbert.py
indexes/
indexes_sapbert/

# Doctor feedback logs — may contain PHI (transcript phrases). Do not commit.
feedback/
feedback_store.json

# Claude Code local config
.claude/

# Scratch / stray files
request.json
retrieval_output.json

# Node / frontend
frontend/node_modules/

# OS junk
.DS_Store
Thumbs.db